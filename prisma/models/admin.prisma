model Admin {
  id        String     @id @default(cuid())
  name      String
  email     String     @unique
  password  String
  role      AdminRole  @default(admin)
  createdAt DateTime   @default(now())
  demoLeads DemoLead[]

  @@map("admins")
}

enum AdminRole {
  super_admin
  admin
}
